# CoffeeManager-OS — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages
#
# Minimum Python version: 3.11

# ── Web framework & scheduling ────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
apscheduler>=3.10.4

# ── OCR engines ───────────────────────────────────────────────────────────────
# docTR (preferred for structured invoices)
python-doctr[torch]>=0.8.1
# EasyOCR (fallback for low-quality / hand-stamped receipts)
easyocr>=1.7.1
python-bidi<0.6.0  # 0.6+ requires Rust/MSVC to build; pin to pure-Python version
Pillow>=10.3.0

# ── XML generation & validation ───────────────────────────────────────────────
lxml>=5.2.1

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1     # optional: alternative to manual settings.env parsing
httpx>=0.27.0            # async HTTP for OpenClaw API calls
